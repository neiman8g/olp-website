---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Button from '../../../components/Button.astro';
import { getProductsByCategory, getCategoryBySlug } from '../../../data/products';
import { company } from '../../../data/company';
import { Image } from 'astro:assets';
import { getProductImage } from '../../../utils/images';
import EmergencyLighting from '../../../components/illustrations/EmergencyLighting.astro';

const category = getCategoryBySlug('emergency-lighting')!;
const categoryProducts = getProductsByCategory('emergency-lighting');

const faq = [
  { question: 'What is a UL 924 emergency lighting inverter?', answer: 'A UL 924 emergency lighting inverter is a centralized power source that provides uninterrupted AC power to emergency lighting fixtures during utility failure. It must reach full output within 10 seconds of power loss and maintain rated output for at least 90 minutes per NFPA 101 §7.9.' },
  { question: 'Centralized vs distributed (battery-pack) emergency lighting — which is better?', answer: 'Centralized inverters like Online Power\'s Power Wave series outlast distributed battery packs 2–3x (10–15 year battery life vs 3–5 years), maintain full brightness to minute 90 (distributed packs dim as they discharge), and replace hundreds of annual fixture tests with a single 90-minute system test.' },
  { question: 'What power range do Online Power emergency lighting inverters cover?', answer: 'Online Power manufactures the widest range from a single US manufacturer: 35W to 400kW. Models include Nano Power Wave (35–100W lithium ion), Micro Power Wave (200–490W), Mini Power Wave (500W–2.7kW), Power Wave 1 (single-phase 2.1–17kW), Power Wave 3 (three-phase 8–50kW), and Power Wave 4 (three-phase 8–400kW).' },
  { question: 'Are Online Power inverters certified for California hospitals (OSHPD/HCAI)?', answer: 'Yes. The SV Power Wave 1, SV Power Wave 3, and SV Power Wave 4 are shaker-table tested and OSHPD/HCAI seismic certified for California hospital construction.' },
  { question: 'Do Online Power inverters work with LED and dimmable fixtures?', answer: 'Yes. All Power Wave models produce pure sine wave output (<3% THD) compatible with every LED driver, fluorescent ballast, and dimmer. Use with the LCR20 load control relay for automatic 0-10V dimmer override during emergency mode.' },
];
---

<BaseLayout
  title="UL 924 Emergency Lighting Inverters | 35W–400 kW | Online Power"
  description="Centralized UL 924 emergency lighting inverters from 35W to 400 kW — the widest range from a single US manufacturer. Pure sine wave, zero transfer time, OSHPD/HCAI seismic certified models. NFPA 101 §7.9 and UL 924 compliant. Built in Los Angeles since 1975."
  faq={faq}
>

  <!-- BREADCRUMB STRIP -->
  <section class="bg-off-white border-b border-steel-gray">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 py-4">
      <nav class="text-[13px] font-display text-slate">
        <a href="/" class="hover:text-steel-blue transition-colors">Home</a>
        <span class="mx-2">/</span>
        <a href="/products" class="hover:text-steel-blue transition-colors">Products</a>
        <span class="mx-2">/</span>
        <span class="text-near-black font-semibold">Emergency Lighting Inverters</span>
      </nav>
    </div>
  </section>

  <!-- ========== HERO — dark with depth ========== -->
  <section class="relative overflow-hidden bg-deep-navy">
    <div class="absolute inset-0 pointer-events-none opacity-[0.08]" style="background-image: linear-gradient(rgba(126,179,245,.7) 1px, transparent 1px), linear-gradient(90deg, rgba(126,179,245,.7) 1px, transparent 1px); background-size: 48px 48px;"></div>
    <div class="absolute -top-32 -right-32 w-[560px] h-[560px] rounded-full pointer-events-none" style="background: radial-gradient(circle, rgba(37,103,194,0.3) 0%, rgba(37,103,194,0) 70%); filter: blur(12px);"></div>

    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 py-14 md:py-20">
      <div class="grid lg:grid-cols-5 gap-8 lg:gap-12 items-center">
        <div class="lg:col-span-3">
          <div class="flex flex-wrap gap-2 mb-5">
            <span class="badge-dark">UL 924</span>
            <span class="badge-dark">UL 1778</span>
            <span class="badge-dark">OSHPD/HCAI</span>
            <span class="badge-dark">ISO 9001</span>
          </div>

          <h1 class="font-display text-4xl md:text-5xl lg:text-[56px] font-extrabold tracking-tight text-white leading-[1.05] mb-5">Emergency Lighting Inverters</h1>
          <p class="text-lg text-muted-blue leading-relaxed max-w-xl mb-8">
            The widest range from a single manufacturer — 35W to 400kW. Pure sine wave output, zero transfer time, built to order in Los Angeles since 1975.
          </p>

          <div class="flex flex-col sm:flex-row gap-3 mb-4">
            <Button href="/contact" size="lg" icon="arrow-right">Size My Inverter</Button>
          </div>
          <p class="font-display text-[13px] text-muted-blue">Responds within 2 business hours</p>
        </div>

        <div class="hidden lg:block lg:col-span-2">
          <EmergencyLighting />
        </div>
      </div>
    </div>

    <!-- Stats bar -->
    <div class="relative border-t border-navy-mid" style="z-index: 2;">
      <div class="max-w-7xl mx-auto px-4 sm:px-6">
        <div class="grid grid-cols-2 md:grid-cols-4 divide-x divide-navy-mid">
          {[
            { label: 'Range', value: '35W – 400 kW' },
            { label: 'Runtime', value: '90 – 120 min' },
            { label: 'Transfer', value: 'Zero (online)' },
            { label: 'Configurations', value: '1Ø & 3Ø' },
          ].map(s => (
            <div class="py-6 px-4 md:px-6 text-center md:text-left">
              <p class="font-display text-[11px] font-semibold uppercase tracking-[0.16em] text-sky-blue mb-2">{s.label}</p>
              <p class="font-display text-[15px] md:text-[17px] font-bold text-white leading-tight">{s.value}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- ========== PRODUCT GRID with phase filter ========== -->
  <section id="inverters" class="py-10 md:py-14 bg-off-white scroll-mt-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6">
      <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 mb-8">
        <div>
          <h2 class="font-display text-2xl md:text-3xl font-bold text-near-black">{categoryProducts.length} Models Available</h2>
          <p class="text-base text-slate mt-1">Click any model for full specs, data sheets, and CAD files</p>
        </div>
        <div class="flex gap-1 bg-white border border-steel-gray rounded-lg p-1" id="phase-filter">
          <button class="phase-btn px-4 py-2 text-[13px] font-display font-semibold uppercase tracking-wide rounded transition-colors bg-steel-blue text-white" data-phase="all">All</button>
          <button class="phase-btn px-4 py-2 text-[13px] font-display font-semibold uppercase tracking-wide rounded transition-colors text-slate hover:text-near-black" data-phase="single">Single Phase</button>
          <button class="phase-btn px-4 py-2 text-[13px] font-display font-semibold uppercase tracking-wide rounded transition-colors text-slate hover:text-near-black" data-phase="three">Three Phase</button>
        </div>
      </div>

      <div class="flex overflow-x-auto snap-x snap-mandatory gap-5 -mx-4 px-4 pb-4 no-scrollbar sm:grid sm:grid-cols-2 lg:grid-cols-3 sm:overflow-visible sm:mx-0 sm:px-0 sm:pb-0" id="product-grid">
        {categoryProducts.map(p => {
          const isThreePhase = /three|3.phase|3Ø/i.test(p.tagline + ' ' + p.name);
          const isSinglePhase = /single|1.phase/i.test(p.tagline + ' ' + p.name);
          const phase = isThreePhase ? 'three' : isSinglePhase ? 'single' : 'all';
          return (
            <a href={`/products/${p.slug}`} class="product-card snap-start shrink-0 w-[82vw] sm:w-auto sm:shrink bg-white border border-steel-gray rounded-lg overflow-hidden group hover:border-steel-blue transition-colors" data-phase={phase}>
              <div class="h-[200px] bg-off-white flex items-center justify-center p-4">
                {typeof getProductImage(p.image) === 'string' ? (
                  <img src={p.image} alt={`${p.name} by Online Power`} class="max-h-full w-auto object-contain group-hover:scale-[1.03] transition-transform duration-300" loading="lazy" />
                ) : (
                  <Image src={getProductImage(p.image) as ImageMetadata} alt={`${p.name} by Online Power`} class="max-h-full w-auto object-contain group-hover:scale-[1.03] transition-transform duration-300" loading="lazy" format="webp" quality={80} widths={[200, 400]} sizes="(max-width: 640px) 100vw, 33vw" />
                )}
              </div>
              <div class="p-5">
                <h3 class="font-display text-xl font-bold text-near-black mb-1 group-hover:text-steel-blue transition-colors">{p.name}</h3>
                <p class="font-display text-[13px] font-semibold text-steel-blue mb-2">{p.tagline}</p>
                <p class="text-[15px] text-slate line-clamp-2 mb-3">{p.description}</p>
                <span class="font-display text-[14px] font-semibold text-steel-blue uppercase tracking-wide">Specs & Data Sheets →</span>
              </div>
            </a>
          );
        })}
      </div>
    </div>
  </section>

  <!-- ========== WHY CENTRALIZED — editorial, no boxes ========== -->
  <section class="py-10 md:py-14 bg-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6">
      <p class="eyebrow mb-4">Why Centralized Emergency Lighting</p>

      <div class="space-y-10">
        <div class="flex gap-5 items-start">
          <div class="w-10 h-10 rounded-lg bg-steel-blue/10 flex items-center justify-center flex-shrink-0 mt-1">
            <svg class="w-5 h-5 text-steel-blue" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
          </div>
          <div>
            <h3 class="font-display text-xl font-bold text-near-black mb-2">Full brightness for the full 90 minutes</h3>
            <p class="text-base text-slate leading-relaxed">Distributed battery packs dim as they discharge, often dropping below the 0.6 fc minimum NFPA 101 requires at the 90-minute mark. Online Power inverters maintain full rated output from first second to last.</p>
          </div>
        </div>

        <div class="flex gap-5 items-start">
          <div class="w-10 h-10 rounded-lg bg-steel-blue/10 flex items-center justify-center flex-shrink-0 mt-1">
            <svg class="w-5 h-5 text-steel-blue" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path></svg>
          </div>
          <div>
            <h3 class="font-display text-xl font-bold text-near-black mb-2">Pure sine wave — every LED driver, every time</h3>
            <p class="text-base text-slate leading-relaxed">Modified sine wave inverters cause LED flickering, buzzing, and premature driver failure. Online Power's pure sine wave output with less than 3% THD is identical to utility power.</p>
          </div>
        </div>

        <div class="flex gap-5 items-start">
          <div class="w-10 h-10 rounded-lg bg-steel-blue/10 flex items-center justify-center flex-shrink-0 mt-1">
            <svg class="w-5 h-5 text-steel-blue" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path></svg>
          </div>
          <div>
            <h3 class="font-display text-xl font-bold text-near-black mb-2">One system replaces hundreds of battery packs</h3>
            <p class="text-base text-slate leading-relaxed">Centralized batteries last 10-15 years versus 3-5 for distributed packs. One annual 90-minute test replaces hundreds of individual fixture tests — cutting maintenance labor by up to 90%.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ========== RESOURCES & LEARNING ========== -->
  <section class="py-10 md:py-14 bg-off-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6">
      <p class="eyebrow mb-8">Resources & Training</p>

      <div class="grid md:grid-cols-3 gap-5">
        <!-- Training -->
        <a href="/training/" class="bg-white border border-steel-gray rounded-lg p-6 group hover:border-steel-blue transition-colors">
          <div class="w-10 h-10 rounded-lg bg-steel-blue/10 flex items-center justify-center mb-4">
            <svg class="w-5 h-5 text-steel-blue" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z"></path></svg>
          </div>
          <h3 class="font-display text-lg font-bold text-near-black mb-2 group-hover:text-steel-blue transition-colors">Product Training</h3>
          <p class="text-[15px] text-slate mb-3">At our plant, virtual, or at your office. Hands-on with our engineering team.</p>
          <span class="font-display text-[14px] font-semibold text-steel-blue uppercase tracking-wide">Register →</span>
        </a>

        <!-- Code Reference -->
        <div class="bg-white border border-steel-gray rounded-lg p-6">
          <div class="w-10 h-10 rounded-lg bg-steel-blue/10 flex items-center justify-center mb-4">
            <svg class="w-5 h-5 text-steel-blue" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path></svg>
          </div>
          <h3 class="font-display text-lg font-bold text-near-black mb-2">Code Requirements</h3>
          <ul class="text-[15px] text-slate space-y-1.5">
            <li>UL 924 — Emergency Lighting Equipment</li>
            <li>NEC Article 700 — Emergency Systems</li>
            <li>NFPA 101 §7.9 — Life Safety Code</li>
            <li>NFPA 110 — Emergency Power Systems</li>
            <li>OSHPD/HCAI — Seismic Certification</li>
          </ul>
        </div>

        <!-- Engineering Support -->
        <a href="/contact/" class="bg-white border border-steel-gray rounded-lg p-6 group hover:border-steel-blue transition-colors">
          <div class="w-10 h-10 rounded-lg bg-steel-blue/10 flex items-center justify-center mb-4">
            <svg class="w-5 h-5 text-steel-blue" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path></svg>
          </div>
          <h3 class="font-display text-lg font-bold text-near-black mb-2 group-hover:text-steel-blue transition-colors">Engineering Support</h3>
          <p class="text-[15px] text-slate mb-3">24-hour load calculations. LED driver compatibility verification. Custom configurations.</p>
          <span class="font-display text-[14px] font-semibold text-steel-blue uppercase tracking-wide">Contact Engineering →</span>
        </a>
      </div>
    </div>
  </section>

  <!-- ========== TESTIMONIAL ========== -->
  <section class="py-10 md:py-14 bg-white">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 text-center">
      <svg class="w-8 h-8 text-steel-blue/30 mx-auto mb-4" fill="currentColor" viewBox="0 0 24 24"><path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10H14.017zM0 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151C7.546 6.068 5.983 8.789 5.983 11h4v10H0z"/></svg>
      <p class="text-xl text-near-black leading-relaxed mb-6 italic">"Online Power has been our go-to for emergency lighting inverters. Their engineering team is responsive and the products are absolutely rock solid."</p>
      <p class="font-display font-bold text-near-black">Alex Jenkins</p>
      <p class="text-base text-slate">Green MEP Engineering</p>
    </div>
  </section>

  <!-- ========== FAQ ========== -->
  <section class="py-10 md:py-14 bg-off-white border-t border-steel-gray">
    <div class="max-w-3xl mx-auto px-4 sm:px-6">
      <p class="eyebrow mb-4">Common Questions</p>
      <h2 class="font-display text-3xl font-extrabold tracking-tight text-near-black mb-8 leading-[1.1]">Emergency Lighting FAQ</h2>
      <div class="space-y-3">
        {faq.map(item => (
          <details class="group bg-white border border-steel-gray rounded-xl overflow-hidden hover:border-steel-blue transition-colors">
            <summary class="flex items-start gap-4 p-5 cursor-pointer">
              <span class="font-display text-[15px] font-semibold text-near-black flex-1 group-hover:text-steel-blue transition-colors">{item.question}</span>
              <svg class="w-4 h-4 text-slate group-open:rotate-180 group-open:text-steel-blue transition-all flex-shrink-0 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
            </summary>
            <div class="px-5 pb-5 text-[15px] text-slate leading-relaxed">{item.answer}</div>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- ========== RELATED CATEGORIES ========== -->
  <section class="py-10 md:py-14 bg-white border-t border-steel-gray">
    <div class="max-w-7xl mx-auto px-4 sm:px-6">
      <p class="eyebrow mb-4">Related Product Lines</p>
      <h2 class="font-display text-2xl md:text-3xl font-extrabold tracking-tight text-near-black mb-8 leading-[1.1]">Explore More from Online Power</h2>
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-5">
        {[
          { href: '/products/category/elevator-backup', name: 'Elevator Backup Systems', desc: 'UL 924 three-phase evacuation power, 24–240 kW.' },
          { href: '/products/category/fire-alarm-backup', name: 'Fire Alarm Backup', desc: 'UL 1481 power for the FACP, 24 hr + 5 min per NFPA 72.' },
          { href: '/products/category/load-control-relays', name: 'Load Control Relays', desc: 'LCR20 UL 924 0–10V dimmer override for emergency lighting.' },
        ].map(c => (
          <a href={c.href} class="group bg-white border border-steel-gray rounded-xl p-6 hover:border-steel-blue hover:shadow-[0_12px_28px_-10px_rgba(37,103,194,0.25)] hover:-translate-y-0.5 transition-all">
            <h3 class="font-display text-xl font-bold text-near-black group-hover:text-steel-blue transition-colors mb-2">{c.name}</h3>
            <p class="text-[14px] text-slate mb-4">{c.desc}</p>
            <span class="font-display text-[12px] font-bold uppercase tracking-[0.12em] text-steel-blue group-hover:text-near-black inline-flex items-center gap-1.5 transition-colors">
              Explore
              <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 5l7 7-7 7"/></svg>
            </span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ========== CTA ========== -->
  <section class="bg-deep-navy py-12 md:py-16">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 text-center">
      <h2 class="font-display text-3xl font-bold text-white mb-4">Ready to Spec Emergency Lighting?</h2>
      <p class="text-base text-muted-blue mb-8 max-w-xl mx-auto">Get a 24-hour load calculation, discuss your project, or request product training.</p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <Button href="/contact" size="lg">Size My Inverter</Button>
        <Button href="/training" variant="ghost" size="lg">Register for Training</Button>
      </div>
    </div>
  </section>

  <!-- MOBILE STICKY CTA -->
  <div class="fixed bottom-0 left-0 right-0 bg-deep-navy/95 backdrop-blur border-t border-navy-mid p-3 z-40 lg:hidden" id="mobile-sticky-cta" style="display:none;padding-bottom:max(12px, env(safe-area-inset-bottom));">
    <Button href="/contact" size="lg" icon="bolt" fullWidth>Request a Quote</Button>
  </div>

</BaseLayout>

<script is:inline>
  // Phase filter
  document.querySelectorAll('.phase-btn').forEach(function(btn) {
    btn.addEventListener('click', function() {
      var phase = btn.getAttribute('data-phase');
      document.querySelectorAll('.phase-btn').forEach(function(b) {
        b.classList.remove('bg-steel-blue', 'text-white');
        b.classList.add('text-slate');
      });
      btn.classList.remove('text-slate');
      btn.classList.add('bg-steel-blue', 'text-white');
      document.querySelectorAll('.product-card').forEach(function(card) {
        var cardPhase = card.getAttribute('data-phase');
        if (phase === 'all' || cardPhase === phase || cardPhase === 'all') {
          card.style.display = '';
        } else {
          card.style.display = 'none';
        }
      });
    });
  });

  // Mobile sticky CTA
  var stickyCta = document.getElementById('mobile-sticky-cta');
  if (stickyCta && window.innerWidth < 1024) {
    window.addEventListener('scroll', function() {
      stickyCta.style.display = window.scrollY > 400 ? '' : 'none';
    });
  }
</script>
